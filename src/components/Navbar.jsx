import "../styles/Navbar.css";

const Navbar = () => {
    return (
        <nav className="navbar">
            <a href="#">Home</a>
            <a href="#">About</a>
            <a href="#">Profiles</a>
        </nav>
    );
}

export default Navbar;